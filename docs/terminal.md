# Common commands
* `cd ..` or `cd folder`
* `mkdir` make directory
* `ls` list content of directory, `ls -a` include hidden files
* `open -a TextEdit filename` opens file in TextEdit
    * use `~/` as root. `~` is typed as `Option+¨` (right to the Å-key)
 